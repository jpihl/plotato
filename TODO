Provide better tools e.g. well documented python functions for insertion and extraction of data.

Cache the plots and provide the user with a refresh button.

Fix the threading error happening when plotting